# Python サーバレス — 詳細ガイドライン

SKILL の本文は `SKILL.md`。このファイルは必要時に読み込む参照層。

## 任意性

この Skill は任意である。クラウドや実行環境が未確定のときは質問ドキュメントでプロバイダ・デプロイ方式を確定してから参照すること。

## 適用条件

- Human または質問ドキュメントの回答で、Python による関数実装が選ばれていること
- ターゲットがサーバレス実行（各クラウドの関数サービス等）であること

## 作業上の原則

- アカウント ID・リージョン・関数名などの識別子はハードコードしない。プレースホルダまたは設定参照にする
- シークレットは環境変数・シークレットマネージャ経由とし、コードやログに出さない
- プロバイダ固有の SDK やイベント型は、選定されたクラウドに合わせて読み替える（このテンプレートはプロバイダ中立の規約のみ示す）

## 実装前に Human へ確認可能な項目

- ランタイムのマイナーバージョンと依存管理方法（レイヤー、コンテナイメージなど）
- 同期/非同期ハンドラ、タイムアウト、メモリ上限
- テスト方針（ローカルエミュレータの有無、モックの範囲）

## 参照

- プロジェクトの spec/ と CLAUDE.md
- test-workflow Skill（テストコマンドの確定）
